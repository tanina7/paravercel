import LoginForm from './LoginForm';

export const metadata = {
  title: 'Iniciar Sesión - Sistema de Trámites',
  description: 'Acceso al sistema de trámites de Universidad del Valle',
};

export default function LoginPage() {
  return <LoginForm />;
}
